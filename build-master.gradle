ext{
    remoteHost = "${remoteHostTradingBot}"
    remotePort = remotePortTradingBot as Integer
    remoteUser = "${remoteUserTradingBot}"
    remotePassword = "${remotePasswordTradingBot}"

    flywayCleanDisabled = true
    snapshot = true
}